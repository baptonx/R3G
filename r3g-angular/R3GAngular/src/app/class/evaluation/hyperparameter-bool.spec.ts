import { HyperparameterBool } from './hyperparameter-bool';

describe('HyperparameterBool', () => {
  it('should create an instance', () => {
    expect(new HyperparameterBool()).toBeTruthy();
  });
});
