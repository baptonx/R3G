import { HyperparameterNumber } from './hyperparameter-number';

describe('HyperparameterNumber', () => {
  it('should create an instance', () => {
    expect(new HyperparameterNumber()).toBeTruthy();
  });
});
