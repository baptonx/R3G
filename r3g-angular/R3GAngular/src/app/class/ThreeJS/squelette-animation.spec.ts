import { SqueletteAnimation } from './squelette-animation';

describe('SqueletteAnimation', () => {
  it('should create an instance', () => {
    expect(new SqueletteAnimation()).toBeTruthy();
  });
});
